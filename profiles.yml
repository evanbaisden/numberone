default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password: poppy
      port: 5432
      dbname: my_dbt_project
      schema: public
      threads: 4
